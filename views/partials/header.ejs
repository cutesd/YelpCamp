<!DOCTYPE html>
<html>
<head>
	 <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1">
	<title>YelpCamp</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
	<link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
		<link rel="stylesheet" type="text/css" href="/stylesheet/main.css">
</head>
<body>
<!-- NAVBAR -->
<nav class="navbar navbar-default">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
	        <span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
			<a class="navbar-brand" href="/campgrounds"><span class="glyphicon glyphicon-tent" aria-hidden="true"></span> YelpCamp</a>
		</div>

		<div class="collapse navbar-collapse" id="navbar">
			<div class="nav navbar-nav">
				<li class="nav-item <%= (typeof page !== 'undefined' && page === 'campgrounds') ? 'active' : '' %>">
					<a href="/campgrounds" class="nav-link ">Campgrounds</a>
				</li>
			</div>	
	
			<ul class="nav navbar-nav navbar-right">
				<% if(!currentUser){ %>
				<li class="nav-item <%= (typeof page !== 'undefined' && page === 'login') ? 'active' : '' %>">
					<a href="/login" class="nav-link">Login</a>
				</li>
				<li class="nav-item <%= (typeof page !== 'undefined' && page === 'register') ? 'active' : '' %>">
					<a href="/register" class="nav-link">Sign Up</a>
				</li>
				<% }else{ %>
				<li class="nav-item">
					<a>
						<% if(currentUser.isAdmin){ %> <strong>ADMIN</strong> <% } %>
						Signed in as: <%=currentUser.username%></a>
					</li>
				<li class="nav-item">
					<a href="/logout" class="class-link">Log Out</a>
				</li>
				<% } %>
			</ul>
		</div>

	</div>
</nav>

<div class="container">
	<% if(error && error.length>0){ %>
	    <div class="alert alert-danger fade in alert-dismissable">
	       <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
	       <%= error %>
	     </div>
	<% } else if (success && success.length>0){ %>
   		<div class="alert alert-success fade in alert-dismissable">
	       <a href="#" class="close" data-dismiss="alert" aria-label="close" title="close">×</a>
	       <%= success %>
	   </div>
  	<% } %>
</div>
<div class="container">